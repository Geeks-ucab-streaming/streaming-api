/*import { Isearch } from "src/core/application/Isearch";
import { FindArtistsInCollectionService } from "./findArtistsInCollection.service";
import { Artist } from "src/artists/domain/artist";
import { Result } from "src/common/domain/logic/Result";
import { GetAllArtistsApplicationService } from "./get-all-artists.application.service";
import { OrmSongRepository } from "src/songs/infrastructure/repositories/song.repository.impl";
import { Song } from "src/songs/domain/song";

export class SearchSongService implements Isearch<Song,Result<OrmSongRepository>> {
  constructor(private readonly song:Song) {}

  async search(song: OrmSongRepository): Promise<Song[]> {
    return await this.search(OrmSongRepository);
  }
}*/